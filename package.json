{
  "name": "how-browsers-work",
  "version": "1.0.0",
  "private": true,
  "description": "最小可用浏览器 — 用于学习浏览器原理",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "jest",
    "browser": "ts-node packages/browser-process/src/BrowserMain.ts",
    "clean": "rm -rf packages/*/dist"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.28.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  }
}
